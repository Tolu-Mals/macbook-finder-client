name: Crawl new data
on:
  schedule:
    - cron: '10 23 * * 1' # Run on 12:10AM every sundays

jobs:
  crawl:
    name: Crawl data and save data
    runs-on: ubuntu-latest
    steps:
    - name: Crawl and save data
      run: curl 'https://macbook-finder-server.onrender.com/crawl'
